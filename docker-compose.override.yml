services:

  app:
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_HTTPS_PORTS=8081   # Внутренний порт HTTPS и он же порт редиректа (открываем только в режиме разработки)
      - ASPNETCORE_Kestrel__Certificates__Default__Path=/app/certs/Ilnitsky.Polls.pfx
      - ASPNETCORE_Kestrel__Certificates__Default__Password=${CERT_PASSWORD}
    volumes:
      - ${APPDATA}/Microsoft/UserSecrets:/home/app/.microsoft/usersecrets:ro
      - ${APPDATA}/Microsoft/UserSecrets:/root/.microsoft/usersecrets:ro
      #- ${APPDATA}/ASP.NET/Https:/home/app/.aspnet/https:ro
      #- ${APPDATA}/ASP.NET/Https:/root/.aspnet/https:ro
      - ${APPDATA}/ASP.NET/Https:/app/certs:ro
    ports:
      - "8080:8080"    # Внешний HTTP
      - "8081:8081"    # Внешний HTTPS

  db:
    ports:
      - "3307:3306"    # Открываем прямой доступ к БД только в режиме разработки
